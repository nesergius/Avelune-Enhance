# Avelune Enhance RC5.2 — UI Overhaul v9.2

Исправления по результатам проверки интерфейса:

- качество одиночной и пакетной обработки по умолчанию — 100%;
- встроенные профили больше не снижают качество сохранения при выборе;
- локальная история по умолчанию отключена и не пополняется без явного включения;
- кнопка раскрытия остаётся доступной после сворачивания панели и меняет подпись для доступности;
- слева под меткой «До» показывается исходник, справа под меткой «После» — готовый результат;
- движок, модели, IPC-контракты и очередь обработки не изменялись.
