# RC6 FIXED5 — compact workflow header

The enhance sidebar header now contains only the centered four-step indicator and the reset button on the right.

Removed visible copy:

- `AI IMAGE STUDIO`
- `Новое улучшение`
- `Четыре шага до готового результата`

The layout uses symmetric 34 px side columns so the step indicator remains optically centered regardless of the reset button. The RC6-specific rule overrides the older responsive rule that hid the indicator below 1100 px.
