"use strict";
const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),os=require("node:os"),path=require("node:path");const {getImageInfo}=require("../src/image-info");
test("JPEG dimensions are found after more than 1MB of metadata",()=>{const dir=fs.mkdtempSync(path.join(os.tmpdir(),"avelune-jpeg-"));const file=path.join(dir,"large-exif.jpg");const parts=[Buffer.from([0xff,0xd8])];for(let i=0;i<18;i++){const seg=Buffer.alloc(65535);seg[0]=0xff;seg[1]=0xe1;seg.writeUInt16BE(65533,2);parts.push(seg);}const sof=Buffer.from([0xff,0xc0,0x00,0x11,0x08,0x01,0x2c,0x01,0x90,0x03,0x01,0x11,0x00,0x02,0x11,0x00,0x03,0x11,0x00]);parts.push(sof,Buffer.from([0xff,0xd9]));fs.writeFileSync(file,Buffer.concat(parts));assert.deepEqual(getImageInfo(file),{format:"jpg",height:300,width:400});fs.rmSync(dir,{recursive:true,force:true});});
